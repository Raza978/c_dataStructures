#ifndef BUBBLESORT_H
#define BUBBLESORT_H

/**
 * @brief Sorts an array of integers in ascending order using the bubble sort algorithm.
 * 
 * @note Bubble sort O(n^2) repeatedly compares adjacent elements 
 * and swaps them if they are in the wrong order. The algorithm will keep going through the list 
 * until the list is sorted.
 */
void BubbleSort();

#endif //BUBBLESORT_H